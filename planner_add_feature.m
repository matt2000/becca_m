function planner = planner_add_feature(planner, k, has_dummy)

% planner.plan{k} = [planner.plan{k}; 0;];
% 
% % if dummy feature is still in place, removes it
% if (has_dummy)
%     planner.plan{k} = planner.plan{k}( 2:end);
% end
